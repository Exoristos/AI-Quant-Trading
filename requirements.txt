# Runtime deps (mirrors pyproject.toml). Used by Streamlit Community Cloud.
#
# PyTorch: default PyPI wheels work on Streamlit Cloud (Linux). For a smaller
# CPU-only install locally or in custom Docker, use PyTorch's index, e.g.:
#   pip install --extra-index-url https://download.pytorch.org/whl/cpu -r requirements.txt
# and pin torch as documented at https://pytorch.org/get-started/locally/
pandas>=2.0
numpy>=1.24
yfinance>=0.2.0
requests>=2.28
torch>=2.0
scikit-learn>=1.3
vectorbt>=0.26
streamlit>=1.28
plotly>=5.18
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0

# AI-Quant-Trading: editable install (import package trading_platform). setuptools for PEP 517.
setuptools>=61
-e .
